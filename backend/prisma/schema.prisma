// prisma/schema.prisma
// Dev: SQLite | Prod: PostgreSQL configuré pour Railway

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  // ... (le reste ne change pas)
